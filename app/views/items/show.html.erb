<h1><%= @item.title %></h1>

<%= render "layouts/subnav" %>

<h3>Related Services</h3>
<ul>
  <% @item.items.each do |related| %>
    <li><%= related.title %></li>
  <% end %>
</ul>

<h3>Related News & Events</h3>
<ul>
  <% @item.items.each do |related| %>
    <li><%= related.title %></li>
  <% end %>
</ul>

<h3>Related Resources</h3>
<ul>
  <% @item.items.each do |related| %>
    <li><%= related.title %></li>
  <% end %>
</ul>

<h3>Related Research</h3>
<ul>
  <% @item.items.each do |related| %>
    <li><%= related.title %></li>
  <% end %>
</ul>